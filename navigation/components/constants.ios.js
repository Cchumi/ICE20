import { Easing } from 'react-native';

// Layout constants
export const center = {
	top: 8,
	left: 0,
};

export const topCenter = {
	top: -110,
	left: 0,
};

export const topLeft = {
	top: -80,
	left: -50,
};

export const topRight = {
	top: -80,
	left: 50,
};

// Style constants
export const bigBubbleSize = 80;
export const smallBubbleSize = 30;
export const bubbleColor = 'rgba(230, 0, 126, 1)';

// Animate Constants
export const animateTime = 800;
export const easingType = Easing.out(Easing.exp);
export const delay = 200;
